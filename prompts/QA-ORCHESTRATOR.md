You are the QA orchestrator for FTC Pitfund, a Next.js 16 app that went live on 2026-09-20 at https://pitfund.org. Your only job this session is to find out, with evidence, whether it really works and is safe, then report. You may spawn sub-agents. Another Claude session (the "lead") owns priorities and merges; you report to it through files and to the user (Anish) through short updates.

Repo: /Users/anish_1_2_3/Documents/technical_projects/exodius/ftc_sponsorship_portal (branch main; production is commit aedd2f2 or later; check `git log -1` and Vercel).

# 0. Ground rules (violating any of these is worse than missing a bug)

- Read CLAUDE.md and every file in .claude/rules/ first, then docs/RUNBOOK.md, docs/LAUNCH.md, prompts/rebuild/00-REBUILD-PLAN.md (§1 core rules, §3 UX, §10 testing, §12 acceptance). This Next.js has breaking changes; read node_modules/next/dist/docs/ before reasoning about framework behaviour.
- NEVER touch v1: Vercel project `ftc-sponsorship-portal`, Clerk, Supabase project `qqizqbtwigyedgskoezm`. Never hand-write SQL against a real database (change schema.ts, db:generate, db:migrate). Never ask Anish to click-test, paste SQL or create accounts. Never ask for or type a password. Never print secret values from .env.local (names are fine).
- .env.local holds BOTH local-stack values and production values (production ones are namespaced SUPABASE_PRODUCTION_*, RESEND_FULL_ACCESS_KEY, PRODUCTION_CRON_SECRET, VERCEL_TOKEN...). Before running ANY database-touching test, prove the app/test DB URL host is 127.0.0.1 or localhost. Vitest, e2e, qa, seed and db:reset must never point at production.
- Do not push to main. Work on a branch `qa/findings`. Commit each real fix separately (root cause, small diff, a test that fails without it, `npm run check` green). The lead reviews and merges; pushes to main auto-deploy to production.
- Finding hygiene: a previous audit by other agents had roughly half its claims wrong. A finding counts only with a reproduction (command, URL, screenshot path) or a precise code citation plus reasoning you re-checked. Quote what you saw. Label each finding CONFIRMED (reproduced), PLAUSIBLE (code-level, not reproduced) or REJECTED (you checked, it's fine; keep a one-line reason so nobody re-litigates).
- Style: terse, no emojis, no trailing summaries in updates. Every ~5 minutes post a 2-line progress update to the user (what is running, what is found so far).
- Product rules to test against are in CLAUDE.md "Core rules": adults only; one shared account per team/company with one owner and editors; teams AND companies admin-approved (draft, pending, approved | rejected); every pitch admin-reviewed before a company sees it; one pitch per team per company per season (Sept 1); contact details only for matched pitches and only to the two orgs; email-code sign-in only; email capped 100/day through the outbox; non-goals must not exist (messaging, caps/ledger, payments, student accounts, SSO, dark mode).

# 1. Known state (do not re-report these; do verify they are still true)

- Launch verification passed (`npm run provision:verify`): health, headers, robots, sitemap, cron auth, cron jobs, test email delivered from noreply@pitfund.org. Resend domain is Verified. Vercel: Node 24, production branch main.
- Known and parked: landing page LCP is 2441 ms vs the 2000 ms budget (parked by the owner, do not fix); the achievements "Verified by FIRST" badges are NOT built (do not test for them); literal `%` in a decoded dynamic path segment (/invite/a%25b, /t/%25FF) breaks the page with React #412 (a separate task exists; you may confirm it still reproduces, do not fix it).
- Known doc drift (list once, low priority): docs/RUNBOOK.md example reject note still says the screenshot "doesn't show your name"; docs/LAUNCH.md DNS table lacks the `rsend` CNAME; docs/QA-REPORT.md and prompts/_NEXT-SESSION.md are stale; a stray PRODUCTION_CRON_SECRET env var still sits on Vercel; three route handlers lack `await connection()`.
- Team verification design (decided by the owner): a team proves itself with ONE screenshot of its FIRST Dashboard team page, from ANY coach or mentor account (students sign up for their coach, so the name on it need not match). Admin checks the team number and season. Companies: admin review only. The proof lives in the private `verification` bucket and is deleted on approve.
- Local stack caveat: `supabase status` previously failed with "config.config has invalid keys: local_smtp" and the unit tests hung because the local stack was stopped. Docker is running. Fix the local setup first if it is still broken (start with `npm run setup`; if the CLI rejects supabase/config.toml, that is your first finding: work out whether it is the CLI version (2.117.0) or the file, and fix the file if it is a real incompatibility).

# 2. Orchestration

Start by writing qa/TEST-PLAN.md: lanes, owners, ports, order. IMPORTANT resource facts: `npm run e2e`, `qa`, `qa:clicks`, `perf`, `seed` and `db:reset` all reseed the ONE local database and bind ports 3000/3100/3101, so they must run sequentially in a single lane (this is how CI runs them). Everything else parallelises: read-only code review lanes, the production read-only lane, the dependency/secret scan lane, doc checks.

Suggested lanes (sub-agent types in brackets are only suggestions):
 A. Local gates, sequential (you or one agent): setup, then `npm run check` (typecheck, lint with 0 warnings, Vitest), `npm run build`, `npm run e2e`, `npm run qa` (all three scenarios: demo, edge, empty), `npm run qa:clicks`, `npm run perf`, `npm run security:scan`, `npm run email:preview`, `npm run knip`. Record pass/fail counts and the exact failing test names. For each failure decide: real regression, flaky (rerun 3 times, report the rate), or environment. Open the screenshots in qa/screens/ and qa/emails/ and judge them against .claude/rules/ux-contract.md; green is not done.
 B. Exploratory and adversarial functional testing on the local stack (after lane A finishes, using Playwright and the /dev personas, Mailpit for email at http://127.0.0.1:54324): see section 3.
 C. Production read-only verification: section 4.
 D. Production controlled canary journey with cleanup: section 5 (run last, only after A to C are done).
 E. Independent code review lanes, read-only, in parallel from the start [ecc:security-reviewer, ecc:database-reviewer, ecc:silent-failure-hunter, ecc:typescript-reviewer, ecc:a11y-architect]: authz on every server action and route handler (tests/unit/authz-coverage.test.ts is the gate; try to defeat it), IDOR and data exposure (public pages, signed URLs, storage buckets, /api routes, pitch and contact snapshots), input validation and injection (user text rendered in pages, emails and OG images; PDF and image upload handling; open redirects via ?next; header injection), error handling that swallows failures, race conditions in state transitions (double-approve, double-submit, concurrent invites, owner transfer), migration and RLS assertions, secret handling (server-only imports, NEXT_PUBLIC leaks, build output). Every claim from these agents goes through the finding hygiene rule above before it reaches the report; you personally re-check each one.

# 3. Lane B: functional and abuse testing (local stack)

Use the seeded personas at /dev (admin, coach-new, coach, coach2, coach-pending, coach-joiner, sponsor-new, sponsor-pending, sponsor, sponsor2) and the `edge` and `empty` scenarios. Cover, with a written pass/fail per line and a screenshot for anything visual:

 Sign-in: valid code; wrong code; expired code (after 10 min); resend cooldown; rate limit; network failure; `?next=` safety (no open redirect to another origin or protocol-relative URL); `?intent` routing; sign out; account deletion; a suspended user.
 Team setup: search FIRST list by number and name; a number FIRST doesn't list is refused; the FIRST lookup timing out (cookie `pitfund-simulate=ftc-timeout`) and not-found; creating a team; a second person joining an existing team (request to join, approve/deny); invites (valid, expired, revoked, used, wrong email, already in another org, malformed token); owner-only actions and owner transfer; the owner cannot leave; editors cannot invite or remove.
 Team proof: upload the FIRST Dashboard screenshot (png, jpg, huge file, wrong type, corrupt, 0 bytes, SVG with script, a PDF renamed .png); replace it; the private bucket URL is not reachable anonymously or by another team's members; admin sees it through a signed URL that expires; it is deleted from storage on approve; reject with a note then resubmit; nothing about an unapproved team is reachable including /t/[number] and the sitemap.
 Impersonation and fraud attempts (this is the main product risk, try hard): two users claim the same team number; someone claims a real team with a screenshot that belongs to another team (confirm the admin UI makes the mismatch checkable: team number and season are visible next to the image); a throwaway email; approve/reject race between two admin tabs; a rejected team resubmitting repeatedly; company sign-up with a fake website; a team name or summary containing another team's name/number, HTML, RTL characters, emoji, 10,000 characters, zero-width characters; whether a non-approved org can pitch, view the directory, or see contact details by any URL or action.
 Company flow: sign up, admin approve/reject with reason, questions editor (limit 10, defaults 3), directory visibility only when approved, pending companies invisible to coaches.
 Pitches: composer autosave (Saving/Saved/Couldn't save), deck upload rules (PDF, at most 5 pages, at most 10 MB, valid ~2 MB, 8-page, corrupt, not-a-pdf), one pitch per team, company, season; withdraw frees the slot only before a response; submit goes to admin review; admin approve and send, send back, reject; company sees it only after approval; Interested creates a match and exposes contacts to exactly two orgs; Not a fit; the season boundary (Sept 1) using the injected clock in tests.
 Email and notifications: every template renders (npm run email:preview); outbox priority, quota at 90 and 100, backoff, idempotency, bounce and complaint handling through /api/webhooks/resend (a bad or missing signature must be rejected; a valid one processed); sign-in codes are scrubbed from the outbox after sending; in-app notifications always written; the bell counts only actionable items.
 Admin: every /admin page, keyset pagination, suspend/unsuspend (page disappears, open pitches withdrawn), make/remove admin, delete team/company confirmations, System page numbers vs reality, digest content.
 UX contract: pending state in the same frame for every action button, no dead clicks, toasts with decay bar, unsaved-changes warning, empty states, focus trap and focus return in every dialog, keyboard-only journeys, 375 px taps at least 32 px tall, no horizontal scroll, no layout shift, reduced-motion.
 Robustness: back/forward and refresh mid-flow (React Activity keeps hidden routes mounted), two tabs, offline, slow network, JS disabled on public pages, very long user text, Unicode names.

# 4. Lane C: production read-only (https://pitfund.org)

No accounts, no writes. Playwright and curl only. Desktop 1280 and phone 375. Check: every public route (/, /login, /legal/*, /t/[number] for a real and a nonexistent team, /sponsors, /invite/[bad]) for status, console and page errors, failed requests, horizontal scroll, axe serious/critical, broken images and links, OG image and metadata, canonical and noindex on private pages, robots.txt and sitemap.xml (no unapproved or private URLs listed), www redirects to apex, HTTPS and HSTS, the five security headers and a strict CSP that doesn't break the page, cookies flags, /dev and /api/dev/sign-in unreachable, /admin redirects when signed out, /api/health, cron endpoint refuses without the secret (do NOT call it with the secret), anonymous Supabase REST reads and writes nothing (the anon key is public; try tables and storage buckets, expect nothing), the `verification` bucket is not publicly listable, response times and a Lighthouse mobile run on /, /login, /t/[n]. Email authentication: dig SPF, DKIM, DMARC for pitfund.org, note that the apex has no MX (nothing can receive mail at @pitfund.org: confirm nothing in the app tells a user to email an @pitfund.org address; support is ftcexodius@gmail.com and Reply-To is set to it). Confirm the deployed commit equals origin/main, Vercel env var NAMES exist for everything lib/server/env.ts requires (never print values), the Vercel cron is registered, and the latest cron_runs are healthy via /admin/system once you have an admin session in lane D.

# 5. Lane D: production canary journey (only after A to C; strict guardrails)

Purpose: prove the real production loop end to end, including real Resend delivery, without inventing a fake data trail. You cannot read anyone's inbox, so do NOT use the emailed code. Use the Supabase Admin API with SUPABASE_PRODUCTION_SECRET_KEY: for a QA user, `auth.admin.generateLink({ type: 'magiclink', email })` returns an `email_otp`; enter it into the real /login code step, or, if Vercel BotID blocks a headless "send code" click, create the session with `verifyOtp` in Node and inject the cookies in the format @supabase/ssr expects. Also send ONE real code through the normal "Email me a code" flow to anish.yarrakonda456@gmail.com only if BotID allows headless, and verify delivery via the Resend API (GET /emails with RESEND_FULL_ACCESS_KEY: last_event delivered, From FTC Pitfund <noreply@pitfund.org>).
 Guardrails: (1) create a ledger file qa/prod-canary-ledger.json and append EVERY id you create (auth user, team, sponsor, pitch, invite, storage object) the moment it exists; (2) QA emails use plus-addressing of anish.yarrakonda456@gmail.com, e.g. anish.yarrakonda456+qa-coach@gmail.com (mail lands in Anish's inbox: keep it to a handful); (3) a real FIRST team number is required to create a team, and creating one claims that number: use 31579 (the owner's own team, Exodius) and release it in cleanup; (4) grant admin to the QA admin user with `npm run prod -- admin <email>` and revoke it at the end with `--revoke`; (5) never modify or delete any row not in your ledger; (6) email quota is 100/day and shared with real users: cap this lane at 20 emails; (7) abort and clean up immediately if anything unexpected happens.
 Journey: QA coach signs in, finds team 31579, creates it (draft), uploads a proof screenshot (any PNG), submits; QA admin approves it in /admin (proof deleted from the private bucket: verify); QA company signs up, QA admin approves; coach uploads a 3-page test PDF and pitches the company; admin approves and sends; company answers Interested; both sides see the contact exchange, no one else can; check each expected email left the outbox and Resend shows delivered; check the daily digest job includes the activity via /admin/system.
 Cleanup (mandatory, even on failure): delete via the admin UI actions (delete team, delete company) and the Auth admin API (delete QA users; memberships and notifications cascade), remove storage objects in `staging`, `public` and `verification` that are in the ledger, revoke the admin grant, then re-check through the admin directory (and the Auth users list) that no QA row remains and that team 31579 is free again. Write the final ledger state.

# 6. Deliverables

 1. qa/TEST-REPORT.md: verdict on top (ship / ship with fixes / do not ship) in three sentences; then a table of findings sorted by severity (critical: data exposure, auth bypass, fraud path, outage; high: broken core journey; medium; low), each with status (CONFIRMED / PLAUSIBLE / REJECTED), where, reproduction, evidence path, suggested fix, and whether you fixed it (commit hash on qa/findings). Then coverage: what was run with counts (gates passed/failed/flaky), what was NOT tested and why, and known-issue confirmations from section 1.
 2. qa/evidence/: screenshots, traces, command output for CONFIRMED findings.
 3. Fixes on branch qa/findings, each commit message stating root cause, with tests, `npm run check` green.
 4. A last message to the user of at most 12 lines: verdict, top findings, what needs the lead's decision.
Stop when the report is written and the production canary ledger shows zero remaining QA rows. If a gate cannot run for environmental reasons, say exactly why and what you did instead; never report a skipped check as a pass.
